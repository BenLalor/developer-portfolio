import type { NextConfig } from "next";

// Local / preview only. Do not add gh-pages, a custom-domain homepage,
// or a production Vercel project until Ben explicitly approves a launch.
const nextConfig: NextConfig = {};

export default nextConfig;
